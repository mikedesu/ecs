#pragma once

#include <cstddef>

typedef struct {
  size_t debug_font_size;
  size_t default_window_width;
  size_t default_window_height;
  size_t window_width;
  size_t window_height;
} gameconfig;
